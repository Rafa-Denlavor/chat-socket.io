{
  "version": 2,
  "devCommand": "npm run dev",
  "builds": [
    {
      "src": "backend/src/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/server.js*" }],
  "installCommand": "npm install",
  "cleanUrls": true
}
